---
title: "6.2 Edge cases and failure modes"
---

## Explanation

An edge case is an input near a boundary of the specification. A failure mode
is a way the code or method can give a wrong, misleading, or unusable result.

Edge cases and failure modes are not only programming details. In scientific
computing, the code can run without an error and still fail scientifically:
the result may not support the claim being made.

It is useful to separate three levels:

- the program crashes or throws an error,
- the program returns a numerically wrong answer,
- the answer is numerically plausible but scientifically misleading.

For an average function, edge cases include an empty array, a one-element array,
negative values, repeated values, very large values, and missing or invalid
values. Failure modes include dividing by zero, silently ignoring invalid data,
or computing an ordinary average when the specification required a weighted
average.

For root finding, edge cases include an interval where the function does not
change sign, a discontinuity, multiple roots, a flat derivative, and a root near
the boundary of the interval. Failure modes include reporting convergence to
the wrong root, stopping because the step size is small even though the
residual is not small, or trusting a result without checking assumptions of the
method.

For Monte Carlo simulation, edge cases include zero samples, very small sample
counts, invalid parameters, a fixed random seed, and repeated independent runs.
Failure modes include insufficient burn-in, strong autocorrelation, a missing
random seed, too few independent samples, or drawing a physical conclusion from
noise.

The important cases depend on the algorithm. A generic checklist is not
enough. For numerical differentiation, dangerous cases include a function that
nearly diverges at one point, a function that is not differentiable at the point
being checked, a step size that is too small and causes cancellation, and a
step size that is too large and gives truncation error.

Correct behavior has two forms. If the assumptions of the method are satisfied,
the code should compute the result within the expected tolerance. If the
assumptions are not satisfied, the code should fail clearly: throw an error,
return a diagnostic value, or refuse to report convergence. The dangerous case
is silent failure, where the calculation continues and produces a plausible but
wrong result.

You can create dangerous cases yourself and ask an AI agent to implement tests
for them. You can also ask an AI agent to propose candidate edge cases and
failure modes for a specific algorithm. In both cases, you must decide whether
the proposed case is mathematically or physically relevant and what correct
behavior should be.

## Things to look up

- Edge case
- Failure mode
- Boundary condition
- Invalid input
- Defensive programming
- Convergence criterion
- Burn-in
- Autocorrelation
- Numerical differentiation
- Singularity
- Cancellation error
- Silent failure

## Exercise

Choose two tasks from the list below. For each task, choose a concrete
algorithm and list at least four dangerous cases:

- computing an average,
- numerical differentiation,
- finding a root of a function,
- running a Monte Carlo simulation.

For each dangerous case, write:

- why the case is dangerous,
- whether the correct behavior is a numerical result, a warning, an error, or
  refusal to converge,
- one fast check that could detect the problem.

Then ask an AI agent to suggest additional dangerous cases for the same
algorithm. Compare its suggestions with your own list, and keep only the cases
whose mathematical or physical relevance you can explain.

## Notes for the exercise

- Include both programming failures and scientific failures.
- Do not list only syntax errors.
- Include cases where the code runs but the result is misleading.
- For root finding, mention at least one assumption of the method.
- For Monte Carlo, mention randomness, sample size, and independence.
- For numerical differentiation, mention step size and behavior of the
  function near the point being differentiated.
- Prefer checks that can run quickly before a long calculation.
- Do not accept an AI-generated test unless you can explain the expected
  behavior.
